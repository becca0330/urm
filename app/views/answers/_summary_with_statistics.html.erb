<%= count %> <%= t(:'answers.results') %>: 
<% for q in questions do 
  	vals=statistic[q.id] || [] # list of answer-values per question
	avg= "%.1f" % ( vals.sum.to_f / vals.size ) # average
	%>
	<%=q.order%>.) <%=q.description%>: avg=<%=avg%> "<%=vals.join('-') %>"
<%end %>