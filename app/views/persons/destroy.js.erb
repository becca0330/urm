$("#summary_person_<%=person.id%>" ).html("<%=escape_javascript( person.summary) %>");
<%# we update summary for staff AND for testusers %>
$("#summary_staff" ).html("<%=escape_javascript( allstaff_summary(person.report) ) %>");
$("#summary_testusers" ).html("<%=escape_javascript( alltestusers_summary(person.report) ) %>");
	
$('#person_<%=person.id%>').fadeOut();
