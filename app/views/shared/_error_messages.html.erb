<% if target.errors.any? %>
  <div id="errorExplanation">
    <h2><%= pluralize(target.errors.count, "error") %> prohibited 
			this record from being saved:</h2>
      <% target.errors.full_messages.each do |msg| %>
        <%= msg %><br />
      <% end %>
  </div>
<% end %>